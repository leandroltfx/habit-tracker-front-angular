import { LoginResponseContract } from './login-response.contract';

describe('LoginResponseContract', () => {
  it('should create an instance', () => {
    expect(new LoginResponseContract()).toBeTruthy();
  });
});
