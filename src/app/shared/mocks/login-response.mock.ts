import { LoginResponseContract } from "../contracts/response/login/login-response.contract";

export const loginResponseMock: LoginResponseContract = new LoginResponseContract(
    'Login efetuado com sucesso!'
);
