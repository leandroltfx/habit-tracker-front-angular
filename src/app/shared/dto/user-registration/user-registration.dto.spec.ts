import { UserRegistrationDto } from './user-registration.dto';

describe('UserRegistrationDto', () => {
  it('should create an instance', () => {
    expect(new UserRegistrationDto()).toBeTruthy();
  });
});
