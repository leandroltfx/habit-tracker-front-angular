import { RegisterUserRequestContract } from './register-user-request.contract';

describe('RegisterUserRequestContract', () => {
  it('should create an instance', () => {
    expect(new RegisterUserRequestContract()).toBeTruthy();
  });
});
