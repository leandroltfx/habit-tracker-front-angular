import { RegisterUserResponseContract } from './register-user-response.contract';

describe('RegisterUserResponseContract', () => {
  it('should create an instance', () => {
    expect(new RegisterUserResponseContract()).toBeTruthy();
  });
});
